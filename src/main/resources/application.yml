server:
  port: 7077

spring:
  application:
    name: trade-broker-service

  datasource:
    url: jdbc:mysql://host.docker.internal:3307/tradding_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
